[package]
name = "stego-sys"
version = "2.0.0"
edition = "2021"
description = "Raw FFI bindings to the stego C ABI (hand-written, no libclang)"
license = "MIT"
links = "stego"

[build-dependencies]
cc = "1"
